name: Production build and deploy

env:
  PACKAGE_PATH: '~'
  DOTNET_VERSION: '6.0.x'

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          
      - name: Build with dotnet
        run: dotnet build --configuration Release

      - name: dotnet publish
        run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/theEstimator

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/theEstimator

  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: .net-app
      - name: create test file
        run: touch test.txt
      - name: create key
        run: touch key.pem
      - name: add secret to key
        run: echo "${{ secrets.SSH_KEY }}" > key.pem
      - name: change permissions
        run: chmod 400 key.pem
      - name: scp test file to vm
        run: scp -i key.pem test.txt ubuntu@3.91.218.170:~/
